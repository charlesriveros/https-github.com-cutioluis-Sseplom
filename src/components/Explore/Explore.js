import React from 'react';
import './Explore.css'

const Explore = (props) => {
    return (
        <section className="seplom-explore">
            <p>{props.title}</p>
        </section>
    );
};


export default Explore;